apiVersion: v1
kind: service
metadata:
  name: nginx-np
spec:
  type: NodePort
  selector: #labels are used as selectors
    project: roboshop
    component: frontend
    environment: dev
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 32141