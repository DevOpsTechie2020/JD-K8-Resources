apiVersion: v1
kind: Pod
metadata:
  name: resources-demo
spec:
  containers:
  - name: resources-demo
    image: nginx
    resources:
      requests:
        memory: "64mi"
        cpu: "100m"
      limits:
        memory: "128Mi"
        cpu: "500m"

